[project]
name = "recallline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "genlayer-test",
]

[tool.pytest.ini_options]
testpaths = ["tests/direct"]
pythonpath = ["."]
addopts = "-q"
